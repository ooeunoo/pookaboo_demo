import 'package:flutter/widgets.dart';

class ToiletBottomSheet extends StatelessWidget {
  const ToiletBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
